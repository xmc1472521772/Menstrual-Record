# 月事记 UI 设计评审与优化方案

- 评审对象：Menstrual-Record（月事记 / Yima）v1.37.0+116
- 评审日期：2026-09-09
- 评审范围：设计 token 体系、5 个主界面、4 个图表组件、深浅双主题、可访问性
- 评审方法：源码静态审查 + WCAG 2.1 对比度实测（脚本计算，非目测估算）

---

## 一、执行摘要

**结论：品牌调性已经成型，工程一致性是最大短板。**

暖陶土色（#B4564F）+ 米白画布的语言在同类产品中有辨识度，token 分层（AppColors / AppDimens / AppThemeColors）的思路正确，性能取舍（去掉 BackdropFilter 与状态卡大投影）也是对的决策。但 token 建立之后没有形成强制约束：业务代码里 65 处硬编码字号、4 套并行的卡片实现、3 份重复空状态、一套从未生效的 NavigationBar 主题。深色模式是在浅色之后"补"出来的，遗留了 20 处浅色固定值。

最需要立刻处理的是**对比度**：本次实测 13 组关键前景/背景组合，仅 2 组达到 WCAG AA 正文标准（15%）。首页状态卡、统计概览卡、AI 入口卡这三块最显眼的渐变卡片上的白色文字，几乎全部不达标。

| 维度 | 评分 | 一句话判断 |
|---|---|---|
| 色彩语义 | 3.5 / 5 | 品牌语言成熟，但对比度普遍性不达标 |
| 排版体系 | 3.0 / 5 | token 已定义，业务层 65 处硬编码绕过 |
| 组件复用 | 2.5 / 5 | 4 套卡片实现；3 个公共组件零使用 |
| 无障碍 | 2.0 / 5 | 对比度大面积缺失，状态仅靠颜色编码 |
| 空间层级 | 3.5 / 5 | 间距节奏统一，首屏密度偏高 |
| **综合** | **2.9 / 5** | **品牌调性已成型，工程一致性拖后腿** |

问题分级统计：**P0（必须修）7 项 · P1（应该修）7 项 · P2（可以修）6 项**。

---

## 二、值得保留的设计决策

先说对的地方，这些在优化中要守住，不要被"重构"误伤：

1. **陶土红替代饱和粉** —— 主色 #B4564F 与米白 #F7F4EE 的组合避开了同类 App 的粉红俗套，成熟且耐看。
2. **`AppThemeColors` 用 ThemeExtension 承载语义色** —— `surfaceCard / onSurface / onSurfaceSecondary / divider` 的分层是对的架构，问题只在于业务层没走全。
3. **用 hairline 描边替代阴影表达层级** —— 卡片 `surfaceCard + divider 描边 + radiusLg(16)`，扁平、克制，符合 Material 3 方向，且省渲染成本。
4. **性能优先的取舍有据可查** —— 状态卡去掉 blur 28 投影、导航栏去掉 BackdropFilter，注释里写清了原因与实测现象。这类"为流畅度牺牲一点质感"的决策是对的。
5. **日历共享核心 `calendar_core.dart`** —— 双日历共用月份数学，避免了双份实现漂移，是本次评审中工程一致性最好的一块。

---

## 三、设计系统审计

### 3.1 颜色 token

| 问题 | 证据 | 影响 |
|---|---|---|
| 渐变末端比起始端更浅，白字对比度崩塌 | `brandPrimary #B4564F → brandLight #C97B6F`（home_screen:375、stats_screen:313） | 渐变末端白字仅 3.21:1；叠加 70~90% 白后低至 2.34:1 |
| 渐变上用"白色透明度"建立层级 | home_screen 456/461/546/594、stats_screen 197 | 陶土红底上任何 <100% 的白色都拿不到 4.5:1 |
| AI 紫色渐变同病 | `aiCardStart #6B5B95 → aiCardEnd #8B7AB8`（app_colors:50-51） | 末端白字 3.77:1，副标题 3.01:1 |
| 三级文本承担了信息功能 | `onSurfaceTertiary #A39A90` 用于计数、时间、说明文字（stats_screen:643、record_screen:607 等） | 白卡上仅 2.77:1，等于"看得见的灰色"实际读不到 |
| 警告色配白字 | `backgroundColor: AppColors.warning`（record_screen:498） | 白字 / #D9A441 = 2.25:1，严重不达标 |
| 深色模式沿用浅色固定值 | 见第五节清单（20 处） | 深色下文字、描边对比度失效 |

**关键约束（实测结论，请写进 AGENTS.md）**：
在 #B4564F 底上，**只有 100% 白能达到 4.5:1（4.79）**；95% 白恰好 4.50，92% 白 4.24，90% 白 4.11，全部不能用于正文。渐变上的层级必须用**字号、字重、留白、分隔线**表达，不能用透明度。

### 3.2 排版

- AppTheme 定义了 11 个文本样式（heading ~ statValue），但业务代码中存在 **65 处硬编码 fontSize**（分布在 15 个文件，home 6 处 / add_record_calendar_page 12 处 / cycle_chart 8 处 / period_length_chart 8 处 …）。
- 实际出现的字号有 **10 种**：10 / 11 / 12 / 13 / 14 / 16 / 18 / 20 / 24 / 32，其中 10、11、13 三个 token 里根本不存在（token 最小是 12）。
- AppBar 标题字号在 `app_theme.dart:45` 写死 20，没有引用 `headingMedium`（20/700），形成"token 之外还有一套标准"。
- 底部导航标签 10px（main_screen:221），低于 11px 下限，在 1.3 倍系统字体下会挤在一起。

### 3.3 间距与圆角

- `AppDimens` 的 4/8/12/16/20/24/32/48 阶梯执行得较好，本次未发现魔法数间距。
- 但存在**未收敛的字面值**：统计页三处 `padding bottom: 100`（stats_screen:114/228/255），与已存在的 `AppDimens.navBarClearance = 100` 同值却未引用。
- **圆角不匹配**：日历"今天/选中"是双层描边——外层 `radiusLg(16)` + 内层 `radiusMd(8)` + 间隙 2（home_screen:1349-1356）。同心圆角规则应为 `外 = 内 + 间隙 = 10`，当前 16 与 8 的差值是 8，视觉上外框"过圆"，两个圆角不同心。

### 3.4 动效

无动效 token。实测出现的时长共 6 种：

| 时长 | 位置 |
|---|---|
| 200ms | 导航图标切换、AnimatedSize、flow 选中态 |
| 280ms | 日历翻页、月份标题淡入、导航滑块 |
| 300ms | 启动页转场、多选日历回位 |
| 600ms | AI 流式光标闪烁 |
| 800ms | 启动页 logo 淡入 |
| 1200ms | 启动页停留 |

同一类交互（导航 vs 翻页）用了 200 / 280 两档，缺少统一节奏。

### 3.5 组件层

| 组件 | 现状 | 判定 |
|---|---|---|
| 卡片 | 4 套并行：`Card`(cardTheme) / Container+surfaceCard+**hairline** / Container+surfaceCard+**divider** / SectionCard | 需收敛为 1 套 |
| `SectionCard` | common_widgets 定义，**零使用** | 死代码 |
| `EmptyState` | common_widgets 定义，**零使用**；stats_screen:269 与 ai_screen:499 各写一份等价实现 | 重复实现 |
| `LegendItem` | common_widgets 定义，**零使用**；home_screen:1389 自写一份 | 重复实现 |
| `StatCircle` | 唯一被复用的公共组件（stats 用 3 次） | 保留 |
| `AppBreakpoints` | 定义 600/840，**零使用** | 预留合理，但需标注 |
| NavigationBar 主题 | app_theme 配置 height 62 + 指示器样式；实际渲染是自绘 `_GlassNavBar`（64） | 主题配置是死代码，且两处高度不一致 |

---

## 四、对比度实测结果（WCAG 2.1）

计算方式：sRGB 相对亮度 → `(L1+0.05)/(L2+0.05)`；半透明白字按实际 alpha 与背景合成后计算。

| 组合 | 位置 | 比值 | AA 正文 4.5 | AA 大字 3.0 |
|---|---|---|---|---|
| 白 / 陶土红实底 | 主按钮、聊天气泡 | 4.79 | PASS | PASS |
| 白 / 渐变末端 #C97B6F | hero 大标题、概览卡标题 | 3.21 | FAIL | PASS |
| 白 70% / 渐变末端 | hero 副文本、"展开"入口 | **2.34** | FAIL | FAIL |
| 白 75% / 渐变末端 | hero 均值标签 | **2.48** | FAIL | FAIL |
| 白 90% / 渐变末端 | StatCircle 标签 | **2.91** | FAIL | FAIL |
| 白 / AI 卡末端 #8B7AB8 | AI 卡标题 | 3.77 | FAIL | PASS |
| 白 80% / AI 卡末端 | AI 卡副标题 | **3.01** | FAIL | PASS |
| 白 / 警告黄 #D9A441 | 异常长度 SnackBar | **2.25** | FAIL | FAIL |
| 三级文本 #A39A90 / 白卡 | 计数、时间、说明 | **2.77** | FAIL | FAIL |
| 二级文本 #6E665E / 白卡 | 通用次级文字 | 5.64 | PASS | PASS |
| 硬编码 inkSecondary / 深色卡 | period_edit_dialog、year_heatmap | **2.80** | FAIL | FAIL |
| 深色三级文本 #827A72 / 深色卡 | 深色下说明文字 | 3.74 | FAIL | PASS |
| 错误红 #B4463F / 深色卡 | 删除操作文字 | **2.92** | FAIL | FAIL |

**13 组中 2 组达标（15%）**，且 5 组低于 3.0——即使按"大号文字"标准也不合格。

补充：日历"安全期"填充 #E2DDD2 在白卡上只有 **1.35:1**，实际观感等于"看不见的格子"；它同时是本应区分度最高的大面积色块之一。

---

## 五、深色模式缺陷清单（附文件:行号）

违反 AGENTS.md 已写明的铁律（`AppColors.ink*` / `canvas` / `tile` / `hairline` 是浅色固定值，禁用于主题表面）：

| 文件:行号 | 问题 | 后果 |
|---|---|---|
| `record_screen.dart:281` | 添加卡片描边用 `AppColors.hairline` | 深色下浅米色描边浮在深色卡上 |
| `record_screen.dart:587` | 历史卡片描边用 `AppColors.hairline` | 同上 |
| `period_edit_dialog.dart:173, 218, 274, 316, 351, 391, 446` | `inkSecondary` 作正文/次级文字 | 深色卡上 2.80:1 |
| `period_edit_dialog.dart:232, 286, 294, 456` | `inkTertiary` 作文字/边框 | 对比度尚可，但语义错误，主题变更时无法跟随 |
| `year_heatmap.dart:146, 160` | `inkSecondary` 作坐标轴/图例文字 | 深色下 2.80:1 |
| `app_theme.dart:407` | 深色输入框 hintStyle 仍用 `inkTertiary` | 与深色语义体系脱节 |
| `app_theme.dart:288` | 深色导航未选中图标用 `inkTertiary` | 6.31:1 尚可，但应走 `onSurfaceTertiary` |

`record_screen.dart:178/185` 与 `home_screen.dart:1312` 虽用了 ink 系列，但位于 `brandSurface` / 排卵色块等**固定浅色面**上，属于允许范围，不计为缺陷。

---

## 六、交互与可访问性

### 6.1 触摸目标（Material 建议 ≥44dp，优选 48dp）

| 元素 | 实测尺寸 | 判定 |
|---|---|---|
| 日历格子（360dp 屏） | 42.3 × 42.3 | 偏小 |
| 日历格子（393dp 屏） | 47.0 × 47.0 | 接近达标 |
| 经量快选按钮 | 高约 33 | **不达标** |
| 底部导航项 | 44 高 × 全宽/4 | 达标 |
| 日历翻页 IconButton | 48 × 48 | 达标 |
| 历史行 PopupMenu | 48 × 48 | 达标 |

### 6.2 状态仅靠颜色编码（WCAG 1.4.1）

日历 5 种日类型全部只用**填充色 + 描边**区分：

- 经期 = 实心陶土红；预测 = 红色描边；排卵 = 实心琥珀；易孕 = 淡绿底；安全 = 浅灰底
- 红绿色盲（男性用户、约 8% 人群）下"易孕绿"与"安全灰"难以区分；"预测描边"与"今天/选中描边"同为红色实线，语义混淆。
- 图例只在日历下方出现一次，扫视时缺乏形状锚点。

### 6.3 可发现性

- **hero 折叠交互无提示**：均值条整块可点（home_screen:429）切换紧凑模式，但没有任何"可折叠"的视觉暗示，仅靠右侧一个 `expand_less` 图标；用户点了才知道。
- **长按查看详情无提示**：日历日详情走 `onLongPress`（home_screen:1031），属于隐藏手势，新用户无法通过界面发现"长按能记经量"。
- **通知入口语义偏移**：AppBar 通知按钮（home_screen:239）点击后跳到设置页，而非通知开关的直接入口，图标与结果不匹配。

### 6.4 语义与无障碍

- `main_screen.dart:159` 已为导航项补了 Semantics，做得对。
- 但日历格子、经量快选、算法选择卡、AI 入口卡均无 `Semantics` 标签；屏幕阅读器读到的是无标签的 GestureDetector。
- 经量快选的选中态只靠背景深浅表达（0.28 vs 0.08 白），无 `selected` 语义。

---

## 七、信息密度与布局

1. **日历固定 6 行带来的空间浪费**：`rows = 6` 硬编码（home_screen:988）。当月只需 4~5 行时，多余 1~2 行留白约 47~94px。这让首屏在"日历 + 状态卡 + 快捷操作"三段式下几乎无法同时容纳。
2. **首屏纵向预算紧张**（360×640 估算）：AppBar 68 + 状态卡 213~283（经期时 +70 经量区）+ 快捷操作 48+16 + 日历卡（标题 48 + 星期 24 + 6 行 254 + 图例 60）≈ 730+ px，必然滚动，而"日历"作为主内容被推到折叠线以下。
3. **层级倒置（设置页）**：分区标题用 `brandPrimary + 18px/600`（settings_screen:91），比卡片内标题 `onSurface + 16px/600` 更大更亮，视觉上"分组名比内容更重"。
4. **三种按钮高度**：主按钮 52（record:325）、快捷操作 48（home:765）、设置页导出按钮走 theme 默认 padding（vertical 16 + 文字 ≈ 52）。同一层级的操作按钮高度不一致。
5. **记录页主次不分**：「添加记录」卡与「历史记录」卡使用完全相同的容器样式与层级，新用户第一眼分不出哪里是操作入口。
6. **无加载/骨架态**：所有数据页只有"空"和"有"两态，首次进入或导入大数据时是白屏直跳。

---

## 八、优化方案

### P0 — 无障碍与深色（建议本迭代完成）

| # | 问题 | 方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| P0-1 | 渐变末端对比度不足 | 渐变由"中→浅"改为"**深→中**"：`brandDeep #8E3F3A → brandPrimary #B4564F`。新增 `AppColors.heroGradient = [brandDeep, brandPrimary]` | app_colors.dart、home_screen:375、stats_screen:313 | 渐变两端白字均 ≥4.5:1（起 7.19 / 止 4.79，已验算） |
| P0-2 | 渐变上白色透明度层级 | 删除所有 `white.withValues(alpha: 0.7/0.75/0.8/0.9)` 文字用法，一律改 100% 白；层级改用字号/字重/分隔线表达 | home_screen:456/461/546/594、stats_screen:197、ai 卡副标题 | 卡上无 <100% 的白色文字 |
| P0-3 | AI 紫渐变对比度 | `aiCardStart #5B4C82 → aiCardEnd #6B5B95`（整体加深一档） | app_colors.dart:50-51、stats_screen:154 | 两端白字 ≥4.5（7.50 / 5.91，已验算） |
| P0-4 | 三级文本失效 | ① `onSurfaceTertiary` 由 #A39A90 改为 **#7A7269**（白卡 4.73）；② 写入规范：**承载信息的次级说明一律用 `onSurfaceSecondary`，tertiary 仅限 placeholder / disabled / 弱化图标** | app_colors.dart:202（深色 #827A72 → **#948B82**，深色卡 4.71） | 全文检索 tertiary 用法，无承载信息的文字 |
| P0-5 | 深色模式浅色固定值 | 按第五节清单逐项替换：`hairline → themeColors.divider`、`inkSecondary → themeColors.onSurfaceSecondary`、`inkTertiary → themeColors.onSurfaceTertiary` | record_screen:281/587、period_edit_dialog ×11、year_heatmap:146/160、app_theme:407/288 | `grep AppColors.ink\|hairline` 在 screens/widgets 下仅剩固定浅色面用例 |
| P0-6 | 危险色深色失效 | 新增 `AppThemeColors.destructive`：浅色 #B4463F / 深色 **#E8908C**（深色卡 6.61:1）；替换硬编码 `AppColors.error` 的文字用法 | app_colors.dart（AppThemeColors 新增字段）、record_screen:836、period_edit_dialog | 深色下删除类文字 ≥4.5 |
| P0-7 | 警告 SnackBar 白字 | #D9A441 底改用**近黑字 #2A2622**（6.67:1），或改为"浅卡 + 琥珀图标 + 深色文字"的 Material 3 反向样式 | record_screen:498 | 警告类 SnackBar 文字 ≥4.5 |

### P1 — 一致性（建议下个迭代）

| # | 问题 | 方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| P1-1 | 4 套卡片实现 | 统一走 `SectionCard`（补 `onTap`/`padding` 参数），删除手写 Container；现有 `Card(...)` 调用改为 `SectionCard` | stats_screen、record_screen、settings_screen、home_screen | Card 容器实现仅 1 处 |
| P1-2 | 3 个公共组件零使用 | stats/home/ai 三处空状态改用 `EmptyState`；图例改用 `LegendItem` | stats_screen:269、home_screen:1389、ai_screen:499 | 空状态实现仅 1 处 |
| P1-3 | NavigationBar 主题死代码 | 二选一：删除 `navigationBarTheme` 配置，或让自绘导航栏读取该主题。建议**删配置**（自绘已成型），并把 `navBarHeight` 作为唯一真源 | app_theme.dart:53/276 | 无重复定义 |
| P1-4 | 65 处硬编码字号 | 补齐 token 缺口：`caption 10 / overline 11 / footnote 13`；逐处替换，AppBar 标题改用 `headingMedium` | 15 个业务文件 | `grep "fontSize:"` 在 screens/widgets 下为 0 |
| P1-5 | 圆角不同心 | "今天/选中"外框 `radiusLg(16)` → **12**（= 内 8 + 间隙 2，取整 12），或内框改 12、外框 16 | home_screen:1351 | 内外圆角差 = 间隙 + 2~4 |
| P1-6 | 动效无 token | 新增 `AppMotion`：`fast 150 / base 200 / slow 280 / page 300`，全局替换字面值 | 新建 constants/app_motion.dart | 动效时长仅 4 档 |
| P1-7 | 层级倒置 | 设置页分区标题改为 `titleLarge(16/600) + onSurfaceSecondary`；如需强调，用 8px 左侧色条而非放大字号 | settings_screen:91 | 分组标题字号 ≤ 卡片标题字号 |

### P2 — 体验与精细化

| # | 问题 | 方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| P2-1 | 日历固定 6 行 | 改为按实际行数（4~6）动态计算高度，配合 `AnimatedSize` 平滑过渡（已有 `monthRowCount`） | home_screen:988 | 4 行月份不出现空白 2 行 |
| P2-2 | 日历格子略小 | 卡内水平 padding 12 → 8，格子由 42.3 → 45.6（360dp 屏）；经量快选 padding vertical 8 → 12（高度 33 → 41，配合 minHeight 44） | home_screen:1000、558 | 主要触摸目标 ≥44 |
| P2-3 | 状态仅靠颜色 | 增加形状通道：预测=**虚线环**（区别于今天的实线环）；排卵=底部中心圆点；易孕=右上小三角；安全=**取消填充**（留白即安全，图例注明"无标记"） | home_screen `_buildDayCell` | 灰度截图下 5 种状态仍可区分 |
| P2-4 | 可发现性 | ① hero 均值条增加"可折叠"提示（首展引导或明确的 chevron 容器）；② 日历首次进入用一次性提示条说明"长按记录经量"；③ 通知按钮改为直接弹出提醒开关，或加 tooltip 说明跳转目标 | home_screen | 隐藏交互均有首次提示 |
| P2-5 | 按钮高度不统一 | 定义三档：`heightSm 36 / heightMd 44 / heightLg 52`，全部主操作按钮走 `heightLg` | home_screen、record_screen、settings_screen | 主操作按钮高度一致 |
| P2-6 | 无加载/骨架态 | 数据加载中的列表页增加 3 行骨架（圆角 8 的浅色条 + 轻微呼吸动画） | record_screen、stats_screen | 首次进入无白屏直跳 |

---

## 九、落地路线

| 迭代 | 内容 | 版本建议 | 验收门禁 |
|---|---|---|---|
| 迭代 1（P0） | 对比度 + 深色修复，纯色值/语义替换，无结构改动 | 1.38.0 | `flutter analyze` 0 issue；175 测试全过；深浅双主题截图逐屏比对 |
| 迭代 2（P1） | 组件收敛 + token 补齐（字号/动效/圆角） | 1.39.0 | 硬编码字号归零；卡片实现归 1；回归测试通过 |
| 迭代 3（P2） | 日历动态行高、多通道状态编码、可发现性、骨架态 | 1.40.0 | 360dp 小屏首屏可见日历；灰度截图可辨状态 |

**建议的门禁脚本**（可加到 CI 或发布前自检）：

```bash
# 1. 禁止在主题表面出现浅色固定值（允许列表：brandSurface 等固定浅色面）
grep -rn "AppColors\.\(ink\|inkSecondary\|inkTertiary\|hairline\|canvas\|tile\)" lib/screens lib/widgets

# 2. 硬编码字号必须为 0
grep -rn "fontSize:" lib/screens lib/widgets

# 3. 渐变卡上禁止半透明白字
grep -rn "white.withValues(alpha: 0\.[0-8]" lib/screens lib/widgets
```

三条命令在迭代 1/2 后应分别返回空。

---

## 十、建议新增的 token 清单

```dart
// app_colors.dart — 语义色补充
class AppColors {
  static const Color heroGradientStart = Color(0xFF8E3F3A); // brandDeep
  static const Color heroGradientEnd   = Color(0xFFB4564F); // brandPrimary
  static const Color aiCardStart       = Color(0xFF5B4C82); // 加深
  static const Color aiCardEnd         = Color(0xFF6B5B95); // 加深
}

// AppThemeColors — 新增字段
//   destructive  浅色 #B4463F / 深色 #E8908C
//   onSurfaceTertiary 浅色 #7A7269 / 深色 #948B82
//   safeDaySurface  浅色 = 透明（留白即安全）/ 深色 = #2A2622

// AppTheme — 字号补缺
static const TextStyle caption  = TextStyle(fontSize: 10, fontWeight: FontWeight.w500);
static const TextStyle overline = TextStyle(fontSize: 11, fontWeight: FontWeight.w500);
static const TextStyle footnote = TextStyle(fontSize: 13, fontWeight: FontWeight.w400);

// constants/app_motion.dart（新建）
class AppMotion {
  static const Duration fast = Duration(milliseconds: 150);
  static const Duration base = Duration(milliseconds: 200);
  static const Duration slow = Duration(milliseconds: 280);
  static const Duration page = Duration(milliseconds: 300);
}

// AppDimens — 补充
static const double controlHeightSm = 36.0;
static const double controlHeightMd = 44.0;
static const double controlHeightLg = 52.0;
static const double minTouchTarget  = 44.0;
```

---

## 十一、需要拍板的两个取舍

1. **渐变保留还是改实底？** P0-1 把渐变改为"深→中"能同时保住层次与对比度，代价是渐变幅度变小、视觉冲击减弱。若接受更强的改动，可直接用实底 `brandPrimary`（4.79:1）+ 右上角一个 12% 白的装饰圆，层次靠装饰而非渐变。我倾向前者（改动小、风险低）。
2. **安全期是否还要填充？** 建议直接取消填充（留白即安全），图例改为"安全期 = 无标记"。这能同时解决 1.35:1 的可见性问题和"安全期灰块让整个日历发灰"的观感问题。若产品坚持要有标记，至少需要换到 #D8D2C6 以上并加虚线边框。

---

## 十二、落地状态（截至 2026-09-09）

- **评审基线**：v1.37.0+116 → **本次优化交付：v1.40.0+119**（P0=1.38 / P1=1.39 / P2=1.40，版本号按语义化规范随迭代递增）。
- **验收结果**：`flutter analyze` **0 issue**；`flutter test` **175/175 全过**（含 main_screen 壳层、widget_test 构建、空状态/骨架态、字号 token 化相关用例）。
- **P0（7 项，全完成）**：渐变「深→中」（hero #8E3F3A→#B4564F / ai #5B4C82→#6B5B95）；删除渐变上半透明白字；三级文本收口（onSurfaceTertiary 浅 #7A7269 / 深 #948B82，仅限 placeholder/disabled/弱图标）；深色 ink*/hairline 逐项替换为 themeColors；新增 `destructive` token（深 #E8908C）；警告 SnackBar 改近黑字 #2A2622。
- **P1（7 项，全完成）**：卡片统一 `SectionCard`（补 onTap/padding）；空状态/图例走公共组件 `EmptyState`/`LegendItem`；删除 `navigationBarTheme` 死代码；65 处硬编码字号归零（补齐 `caption 10 / overline 11 / footnote 13`，新增 `micro 9 / displayXl 36 / display2xl 56`）；日历「今天/选中」圆角同心（外框 12 = 内 8 + 间隙 2）；动效 `AppMotion`（fast150/base200/slow280/page300）四档；设置页分区标题层级倒置修复。
- **P2（6 项，全完成）**：日历按实际行数动态高度（monthRowCount + AnimatedSize）；格子/经量快选触摸目标 ≥44；状态多通道编码（预测虚线环 / 排卵圆点 / 易孕三角 / 安全留白）；可发现性提示；按钮高度三档 `controlHeightSm 36 / Md 44 / Lg 52`；加载骨架 `SkeletonList`（record/stats 接 `isLoading`）。
- **门禁脚本（第九节）**：三条 `grep` 在 `lib/screens`、`lib/widgets` 下均已返回空（仅 `app_theme.dart` 内 token 定义本身保留），满足全部验收门禁。

*评审人：UI Designer · 所有对比度数值均由 WCAG 2.1 公式实测，建议色值均已回验达标。*
