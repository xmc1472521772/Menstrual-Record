# 月事记（Menstrual-Record）全面代码审查与优化建议

> 审查日期：2026-09-08 · 基线版本 1.33.4+107  
> 范围：lib/ 全部 32 个文件（16,048 行）+ test/（1,878 行）+ pubspec + Android 构建配置  
> 结论先行：`flutter analyze` 0 issue，整体质量高于同类个人项目均值。本次发现的问题以**结构性可维护性**和**少数真实性能热点**为主，无阻断性缺陷。

---

## 一、总体评价

### 已做对的事（不需要再动）

| 领域     | 现状                                                                                                             |
| ------ | -------------------------------------------------------------------------------------------------------------- |
| 渲染性能   | 整数 dayKey 索引（O(1) 经期查询）、格子级 ValueNotifier 局部刷新、RepaintBoundary 隔离、状态卡去 boxShadow、导航栏去 BackdropFilter——均有注释说明动机 |
| 数据防线   | 导入 10MB 限制、脏数据归一化（fromJson）、跨表原子替换事务、写操作重入门闩                                                                   |
| 状态管理   | DAO/Provider 构造注入可测；dataVersion 单调版本号供 Selector 精准重建                                                           |
| 时健性    | 午夜定时器 + 前台恢复双重跨天检测；时区初始化 Completer 防竞态                                                                         |
| AI 鲁棒性 | 重试分类（retryable）、think 标签剥离、JSON 三级修复、报告持久化缓存                                                                   |

### 测试与静态检查现状

- `flutter analyze`：**0 issue**
- 测试 8 个文件覆盖 provider / prediction / utils / AI 解析 / 部分屏幕；缺口见 P1-9

---

## 二、优化项（按优先级）

### P0-1 AI 聊天流式渲染：每个 SSE chunk 触发整屏重建 + Markdown 全文重解析

- **位置**：`ai_assistant_screen.dart` `_sendChatMessage()`（L202-215）：每收到一个 chunk 就 `setState` 重建 `ChatMessage` 对象；最长的助手消息经 `MarkdownBody` 全文重新解析。
- **影响**：流式输出期间帧率随消息变长持续劣化，低端机明显掉帧；这是当前代码里最真实的性能热点。
- **方案**：① 节流合并——buffer 累积、50~100ms 定时 flush 一次 UI；② 流式中的最后一条气泡改用 `ValueListenableBuilder` 局部刷新，不整屏 setState；③ 报告/气泡的 Markdown 解析结果按 dataVersion 缓存。
- **预期收益**：打字机效果恒定帧率；长回答时 UI 线程占用显著下降。

### P0-2 每次数据变更都全量导出 JSON 写自动备份

- **位置**：`period_provider.dart` `_commitMutation()` → `_autoBackup()`：任何一次记录写操作 = 一次 DB 写 + 全量记录/经量序列化 + 文件 IO + 目录排序清理。`exportData()` 内部还会再做一次 `_flowDao.getAll()`。
- **方案**：备份去抖（如 30 秒合并窗口）或改为每日首次变更时备份；序列化放 `Isolate.run`。注意保留"多选日历批量保存只触发一次"的现有优点。
- **预期收益**：连续操作时消除 IO 峰值；写路径耗时更平稳。

### P0-3 冷启动设置读取串行低效 + 双份缓存

- **位置**：`settings_provider.dart` `loadSettings()` 8 次串行 DB 查询；`period_provider.dart` `_performLoad()` 又独立读 3 次设置。同一设置值（周期/经期天数）在两个 Provider 各存一份。
- **方案**：① `SettingsDao.getAll()` 已存在但未被复用——`loadSettings` 改为一次查询；② `PeriodProvider` 启动时从 `SettingsProvider` 内存取值（构造注入或启动时序保证），消除双份来源。
- **预期收益**：启动路径 DB 往返 11+ → 2~3 次；消除"设置改了但预测没跟随"这类一致性隐患的土壤（此前日历延展 bug 即同类问题）。

### P0-4 依赖健康：1 个停更包 + 若干显著落后

- `flutter pub outdated` + analyze 证实 **"1 package is discontinued"**：`flutter_markdown 0.7.x` 已被 Flutter 官方停止维护，且它是 AI 聊天渲染的核心依赖。
  - 方案：迁移社区续维护的 `flutter_markdown_plus`（API 兼容 fork，改动极小）或 `markdown_widget`。
- `cupertino_icons`：全库 0 引用，可移除。
- 版本落后（当前 → 最新）：`share_plus 7.2 → 13`、`file_picker 10.0（被精确 pin）→ 12`、`flutter_local_notifications 17 → 22`、`flutter_lints 4 → 6`。
  - 方案：按 minor 策略逐个升级；`share_plus` 跨大版本有 Android Intent 行为变化，升级后需回归导出/分享功能；`file_picker` 的精确 pin 建议改为 caret 并锁 pubspec.lock。
- **预期收益**：消除停更包的兼容/安全悬置；避免未来 Flutter SDK 升级时被旧插件卡死。

### P1-5 PeriodProvider 职责过载（架构）

- 现状 1,155 行承载 9 类职责：记录 CRUD、日经量、设置缓存、**AI 报告缓存 + 聊天历史**、小组件同步、通知调度、自动备份、午夜定时器。
- 方案：拆出 `AiAssistantProvider`（报告缓存/聊天历史/数据版本比对）与 `BackupService`。PeriodProvider 回归"记录 + 预测"。
- 预期收益：聊天状态变更不再耦合经期数据监听链；AI 缓存可独立单测；为 P1-8（聊天持久化）铺路。

### P1-6 巨型 UI 文件拆分（可维护性）

- `ai_assistant_screen.dart` 1,969 行、`record_screen.dart` 1,532 行、`home_screen.dart` 1,447 行。
- 建议拆：record 的编辑对话框（约 600 行，内含校验/心情/症状/备注）→ `widgets/period_edit_dialog.dart`；AI 报告的 7 类卡片 → `widgets/report_cards.dart`。
- 预期收益：单文件职责清晰、增量编译更快、编辑对话框可复用于日历详情入口。

### P1-7 代码重复

- `PeriodProvider.isSafeDay()` 与 `_isSafeDayDirect()` **逻辑完全重复**（L989-1000 vs L1037-1046），前者只是多了已被调用方保证的前置判断。
- 经量选项 `(String, int)` 列表在 `home_screen.dart` 内联定义两处（状态卡 + 日详情弹窗）；日类型中文标签 map 也是内联。
- 方案：统一收敛到常量/单一私有方法。
- 预期收益：改一处生效一处，消除"改了弹窗忘了状态卡"的回归面。

### P1-8 聊天历史不持久化（产品+技术交叉项）

- 报告已持久化到 SQLite（`ai_report_json`），但聊天历史仅内存，进程被杀即丢。
- 方案：与报告同模式持久化（`ai_chat_json`，上限保留最近 N 轮），或最低成本在 UI 明示"对话不会被保存"。
- 预期收益：消除"我上次问过什么"的体验断裂；与报告缓存的心智一致。

### P1-9 测试缺口

现有测试质量好（provider 645 行），但关键防线缺锁定：

| 缺口                               | 为什么重要                                                    |
| -------------------------------- | -------------------------------------------------------- |
| DB 迁移 v1→v4                      | onUpgrade 逻辑无回归保护，未来加 v5 时易破坏老用户数据                       |
| import/export 往返                 | 数据安全关键路径；overwrite 事务、重叠过滤行为应被测试固化                       |
| `WidgetService._buildWidgetJson` | 原生小组件依赖该 JSON 契约，字段变更会静默破坏小组件（建议先设 `@visibleForTesting`） |
| 通知调度决策                           | "预测日期未变跳过调度 / 无预测清理残留"是行为约定，值得锁定                         |
| AI 屏幕 UI 冒烟                      | 流式节流改造（P0-1）前先补基础测试                                      |

### P2（打磨项，顺手做）

1. `_dayTypeCache` 只增不减（跨月翻页累积；量级小，可在翻页清理 notifier 时顺带剪枝）。
2. 散落硬编码文案（'进行中'、'删除记录'、'心情/症状/备注'、'距下次经期还有 X 天'等）未进 `AppStrings`——与既有约定不一致。
3. `_GlassNavBar` 用 GestureDetector 无 `Semantics`，无障碍可补标签。
4. `ai_assistant_screen.dart` 大量 `var themeColors` / `dynamic` 参数，改为 `AppThemeColors` 类型可让编译器抓拼写错误。
5. `record_screen.dart` 文件末尾多余空行（L1533）。

---

## 三、新功能建议（严格基于"本地优先、隐私安全、记录+预测+AI"定位）

| 优先级    | 功能                   | 应用场景                                                       | 价值                                                                                   | 工程量                                  |
| ------ | -------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------ |
| **P0** | **每日症状/情绪速记**        | 经前一周乳房胀痛、排卵期不适、情绪低落——当前症状/心情只能"按次"记一条，日级体验无处安放（日详情弹窗仅能记经量） | ① 补齐"日历点哪记哪"闭环；② AI 报告的症状趋势从按次升级为按日，可识别 PMS 模式，直接提升报告质量；③ 复用 daily_flows 表模式与日详情弹窗入口 | 中低（DB 迁移 v5 加 `daily_logs` 表 + 弹窗扩展） |
| **P1** | **应用锁（PIN/生物识别）**    | 经期数据高度敏感；借手机给他人场景                                          | 把"隐私安全"从口号变成可见卖点，女性健康类 App 标配                                                        | 中（`local_auth` + 启动门卫页）              |
| **P1** | **就医问诊摘要导出（PDF/长图）** | 报告已有"就医提醒"，但真去医院时医生要的是结构化历史：周期长度序列、症状频次、经量分布               | 把 AI 报告/统计转化为"问诊资产"；复用现有 share_plus 与统计数据                                            | 中（`pdf` 包渲染模板）                       |
| **P1** | **提醒体系升级**           | 现仅"经期来临前 N 天"一条。可加：经期中每日记录提醒、排卵期提示（可开关）                    | 记录完整性是预测准确性的前提；每日提醒直接提升经量/症状数据留存                                                     | 中（NotificationService 多通道）           |
| **P2** | **AI 报告历史与评分趋势**     | 报告已持久化但只存最后一份；每次生成的 healthScore + 时间入列，统计页加"健康评分趋势"        | 单次报告 → 长期追踪，强化"记录越久越有价值"的留存逻辑                                                        | 低                                    |
| **P2** | **AI 报告历史与评分趋势**     | 报告已持久化但只存最后一份；每次生成的 healthScore + 时间入列，统计页加"健康评分趋势"        | 单次报告 → 长期追踪，强化"记录越久越有价值"的留存逻辑                                                        | 低                                    |
| **P2** | **月视图桌面小组件**         | 现有小组件是状态卡；月视图可直读日历                                         | 快捷场景延伸                                                                               | 高（原生 AppWidget 重写，放最后）               |

### 明确不建议引入（与定位无关，主动划出边界）

- ❌ 社区/树洞等社交功能——与本地隐私定位冲突，内容合规成本高
- ❌ 云端账号同步体系——本地优先是差异化卖点；最多做"用户自选文件夹备份"
- ❌ 饮食/喝水打卡等泛健康功能——偏离经期核心，摊薄焦点
- ❌ 试纸拍照识别等 ML 重功能——投入产出比不成立

### 隐私强化（AI 功能与产品定位的张力，建议同步处理）

当前发送给云端模型的数据文本包含**全部备注原文**（notes 可能含身份信息）与逐日经量明细，而产品自我定位是"本地存储，隐私安全"。建议：

1. 首次使用 AI 功能前加一页显式同意说明（数据将发送至所选模型服务商）；
2. 提供"脱敏发送"选项：剥离备注、仅发送统计摘要与日期区间；
3. 设置页提供 AI 功能总开关，关闭后不出现任何网络请求。

---

## 四、建议执行顺序

1. **第一批（低风险高收益，1 个版本内）**：P0-1 流式节流、P0-2 备份去抖、P0-3 设置批量加载、P0-4 移除 cupertino_icons + flutter_markdown 迁移
2. **第二批（结构期，需先确认再动手）**：P1-5 拆 AiAssistantProvider、P1-6 巨型文件拆分、P1-7 去重、P1-9 补测试
3. **第三批（功能期）**：每日症状速记（新功能 P0）→ 应用锁 → 问诊导出
4. P2 项随手顺手修，不单开版本

> 按项目版本规范：优化类走 patch，功能新增走 minor，build 号同步递增。
