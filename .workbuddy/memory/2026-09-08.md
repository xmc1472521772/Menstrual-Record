# 2026-09-08

## Bug 修复：小组件修改经量后进入 App 不同步（1.33.1+104）

**现象**：App 在后台，用户在桌面小组件上把经量从"少"改为"多"，再点小组件进入 App，App 仍显示"少"；反向（App 改 → 小组件同步）正常。

**根因**：`app.dart` 的 `_MyAppState` 中三条小组件→App 同步路径（`dataChanged` 回调、启动检查、`resumed` 恢复检查）都用 `context.read<PeriodProvider>()`，但该 context 位于 `MultiProvider` **上方**（Provider 在其 `build()` 内部创建），查找必然抛 `ProviderNotFoundException` 且被静默吞掉。更糟的是 `dataChanged` 回调里 `clearDataDirty()` 在抛异常之前执行，把 dirty 标记清掉了，连"恢复前台兜底检查"也被破坏。

**修复**（仅改 `lib/app.dart`）：
- Provider 改为在 `initState` 中创建（`late final` 字段，`PeriodProvider()..loadRecords()` / `SettingsProvider()..ensureLoaded()`）
- `MultiProvider` 用 `ChangeNotifierProvider<T>.value` 注入同一实例
- 三条同步路径直接引用 `_periodProvider` 字段，不再走 context 查找
- `dispose()` 中手动 dispose 两个 Provider（`.value` 注入不会自动释放）

**教训（重要）**：在创建 Provider 的 State 生命周期回调里不能用 `context.read`；Provider 查找只向上、不向下。要么把 Provider 提到上方，要么直接持有实例。

**附带**：补齐两个测试 mock（`home_screen_test.dart` / `add_record_calendar_test.dart`）缺失的 `refreshConnection` 实现（今早加接口时漏的，导致 analyze 报 error、测试无法编译）。

**验证**：analyze 0 error（剩 2 条存量 info）；flutter test 75/75 通过；release APK 构建成功（58.7MB）。

## ⚠️ 事故：.git 对象库损坏与恢复

**起因**：验证"存量问题"时在复合命令里跑了 `git stash`（想对比改动前 analyze 结果），命令被 SIGTERM 中断于 stash/gc 过程中，导致 `.git/refs` 目录被删、主 pack 文件丢失（只剩 .idx）、本地全部历史提交对象丢失。仓库一度报 "not a git repository" / "bad object HEAD"。

**恢复过程**：
1. 备份 .git → `C:/Users/xmc/Documents/workspace/vibecoding/Menstrual-Record-git-backup-20260908/`
2. 有个 fetch 进程自动从 origin 下载了对象（pack-a97f77a6，仅含 origin 上的 first commit）
3. `mkdir .git/refs/{heads,tags,remotes/origin}`，按 reflog 恢复 `refs/heads/main`（写 loose ref 文件）
4. 删除损坏 index，`git reset` 到 origin/main（21b3aaf "first commit"）
5. 移走孤儿文件（pack-4d52….idx、multi-pack-index）到 /tmp
6. `git add -A` + 提交恢复快照 e818bc5（含全部当前代码 + 本次 bug 修复），随后 d8d9b29 提交测试 mock 修复

**结果**：代码零丢失（工作区一直完好），但**本地提交历史不可恢复**（origin 上只有 first commit，从未推送）。丢失提交的消息记录仍可在 `.git/logs/HEAD`（115 条 reflog）中查到。

**教训（铁律）**：
- **永远不要在这个项目跑 `git stash`**（尤其复合命令里）；要临时对比用文件拷贝
- **必须定期推送 origin 备份**（当前 origin 只有 first commit，本地历史全靠单机）
- git 命令被中断后要立即检查 `git fsck` + refs 完整性

## 沙箱测试环境备忘

- `flutter test` 需三重绕过：`env 'PROGRAMFILES(X86)=C:/Program Files (x86)'`（bash 的 export 不接受带括号变量名，**必须用 env 命令注入**）、清空代理环境变量、`PATH` 前置 `C:/Users/xmc/.workbuddy/binaries/python/versions/3.13.12/DLLs`（sqlite3.dll 所在，已确认）
- 测试中 WidgetService/path_provider 的 MissingPluginException 是正常噪音（已捕获）

## ✅ 更正（13:10）：历史大部分已恢复！

用户指出"昨天已推送 GitHub"。核实属实：origin/main reflog 显示多次 `update by push`，昨晚 23:00 推到 `e3cbbda`。事发时的"历史全丢"是误判——refs 被删后 git 退回陈旧 packed-refs（21b3aaf），而自动 fetch 在 12:51 才把 origin/main 更新到 e3cbbda，我检查的时机在更新之前。

**最终恢复结果**：
- fetch 已把 GitHub 全部对象拉回本地（pack-a97f77a6）
- main 重置到 e3cbbda：**104 个提交完整恢复**（rev-list 验证）
- 今日 11 个未推送提交的对象仍丢失，但全部代码改动在工作区，已合并提交为 `97133cb`（消息中附 11 条原提交消息）
- 历史总数 105；工作区干净（仅 .workbuddy/ 不入库）
- 之前的快照提交 e818bc5/d8d9b29 成为孤儿对象，留在 reflog

**新教训**：判断远程真实状态用 `git ls-remote` 直查；本次靠用户纠正才发现误判。

## 推送完成（13:35）

- 第一次 push 被 GitHub Push Protection 拦截：OpenRouter API Key 硬编码在 `ai_health_service.dart:312`（今日新增的 OpenRouter 功能引入；已确认昨晚推送版本不含该 key，**从未公开**）
- 按用户选择方案A：改为 `String.fromEnvironment('OPENROUTER_API_KEY', defaultValue: '')`（与文件内 GLM key 写法一致），amend 进恢复提交 → 新 SHA `d134dc7` → push 成功
- 本地构建使用 key：`--dart-define=OPENROUTER_API_KEY=xxx`（run/build 都要加，否则 OpenRouter 模型显示未配置，代码有 isEmpty 兜底不会崩）
- **异常现象**：`refs/remotes/origin/*` 的 loose ref 写入被实时回滚（update-ref exit 0 + GIT_TRACE_REFS 事务成功，但文件毫秒级消失；连全新 ref 名也一样），怀疑 IDE git 自动刷新或安全软件实时防护，未能定位根因
- **有效解法**：直接 sed 修改 `packed-refs`（其写入稳定），已把 origin/main 修正为 d134dc7，status 显示完全同步
- 最终状态：GitHub main = d134dc7（105 个提交完整历史），本地一致，工作区干净
