# 项目布局分析报告 — 月事记 (Menstrual-Record)

> 分析日期：2026-09-09 ｜ 基线版本：1.36.1+115 ｜ 范围：`lib/` 全部 37 个 Dart 文件（约 17,890 行）
> 方法：全量阅读 4 个 tab 屏 + AI 屏 + 导航壳 + 主题/token 三件套 + 组件库，交叉 grep 量化硬编码分布。**本报告仅分析，未改动任何代码。**

---

## 一、现状盘点

### 1.1 页面 / UI 结构

```
MyApp (app.dart)
├─ 全局字体钳制 1.0~1.3 (MediaQuery.withClampedTextScaling)
├─ MultiProvider: PeriodProvider / SettingsProvider / AiAssistantProvider (.value 注入)
└─ SplashScreen → MainScreen
    ├─ IndexedStack 保活 4 tab（滚动位置/选中态不丢）
    ├─ HomeScreen      1442 行  AppBar(68) + 状态hero卡 + 快速操作 + 自绘日历(PageView) + FAB
    ├─ RecordScreen     837 行  今日条 + 添加卡 + 历史卡（单一 ScrollView）
    ├─ StatsScreen      843 行  TabBar 3 页：概览 / 趋势(4 张 CustomPainter 图表) / 历史
    ├─ SettingsScreen   802 行  周期参数 / 提醒 / 数据管理（导出/导入/恢复备份）
    └─ AIAssistantScreen 1542 行 (push)  报告 + 聊天双 Tab、流式回放、模型选择、会话管理
```

### 1.2 组件与目录组织

| 目录 | 文件数 | 行数 | 内容 |
|---|---|---|---|
| `screens/` | 7 | ~6,580 | 4 tab + AI 屏 + 多选日历页 + Splash |
| `widgets/` | 9 | ~3,890 | common_widgets(4 组件) + 4 张 CustomPainter 图表 + trend_chart_shared + report_cards + period_edit_dialog |
| `providers/` | 3 | ~1,880 | Period(1139) / AiAssistant(518) / Settings(219) |
| `services/` | 5 | ~3,500 | ai_health(1714) / prediction / notification / widget / backup |
| `constants/` | 3 | ~1,020 | app_colors.dart 内含 **AppColors + AppShadows + AppDimens + AppThemeColors** 三套 token；app_theme.dart 文字刻度 12 档 |
| `database/` | 5 | ~660 | DatabaseProvider 抽象接口 + DAO 注入 |
| `models/` `utils/` | 5 | ~480 | 手写序列化 + 日期工具 + 校验纯函数 |

### 1.3 值得肯定的既有实践（本次分析的前提，不建议推翻）

- **token 体系完整**：颜色/间距/圆角/阴影/文字刻度均有归口，深浅主题靠 `AppThemeColors` ThemeExtension + lerp 正确实现；
- **性能意识强**：RepaintBoundary 分区、去掉导航栏 BackdropFilter、日历格子 ValueNotifier 局部刷新、`_DayCellPalette` 主题色快照——都是有注释、有因果的决策；
- **跨机型一致性**：内置 MiSans 子集 + 根节点字体缩放钳制 + TextPainter 显式 fontFamily；
- **DI 与测试**：DAO/Provider 构造注入，13 个测试文件覆盖 provider/service/纯函数层。

以下问题均在此基础上的"增量改进"，非架构翻盘。

---

## 二、问题清单（五维）

### A. 视觉层次

#### A1【P0】浅色专用色常量误用于主题相关表面 → 深色模式对比度失效

- **现象**：`AppColors.ink / inkSecondary / inkTertiary / canvas / hairline / tile` 是"浅色主题固定值"，但 screens 内有 **36 处**直接用它们给主题相关表面上色：record_screen 16 处、home_screen 12 处、ai_assistant_screen 8 处。
- **典型病例**：
  - `record_screen.dart:237` 无经期今日条：文字 `AppColors.ink`(#2A2622 深墨) 叠在 `themeColors.surfaceTile`（深色模式 #2F2A27）上——**对比度 ≈ 1.1:1，深色模式下不可读**；
  - `record_screen.dart:371/380/387` 添加卡日期行：inkSecondary/inkTertiary 在深色 surfaceCard 上同样失效；
  - `home_screen.dart:245` AppBar 通知图标 `AppColors.ink`，深色主题 AppBar 背景为 #141211——图标近乎隐形；`:822/849` 日历翻页箭头 inkSecondary 在深色卡片上对比度约 2.4:1（低于 AA 正文 4.5:1）。
- **原因**：AppThemeColors 语义色是后期引入的，迁移时只替换了"作者当时正在改"的文件，没有做全库扫描收尾；`AppColors.ink*` 与 `themeColors.onSurface*` 命名相近，极易混用且 analyzer 不报错。
- **建议**：
  1. 逐处替换规则——背景是 `themeColors.*` / AppBar / Card 主题面的，一律改 `context.themeColors.onSurface / onSurfaceSecondary / onSurfaceTertiary`；背景是 `AppColors.brandSurface / brandSoft` 等固定浅色容器的可保留（record_screen:178 即属此类合法用法）；
  2. 替换后真机深色模式走查 4 tab + 2 弹窗；
  3. 在 AGENTS.md 的 Known Traps 里加一条"禁止在主题相关表面使用 AppColors.ink*/canvas/tile/hairline"防复发。
- **预期效果**：深色模式正文对比度从 ~1.2:1 提升到 ≥4.5:1（WCAG AA）；这是一处**正确性缺陷**而非纯审美问题。

#### A2【P1】三种"卡片表面"并存，层级语义不统一

- **现象**：同一应用里卡片有三种画法——① `Card` 组件（主题定义：hairline 描边 + radiusLg 16）；② `SectionCard`（black 5% 阴影 + radiusMd 12）；③ 手写 Container（home 日历卡描边 radiusLg、hero 渐变 radiusXl 20、AI 卡渐变 radiusLg）。圆角 12/16/20 混用，阴影/描边/渐变三种"浮起"手段随机出现。
- **原因**：SectionCard/阴影风格是旧设计语言遗产，A1 扁平卡片语言（注释可见）落地后未回收旧组件。
- **建议**：定义显式表面层级表（层级 0 = 画布；层级 1 = surfaceCard + hairline 描边 + radiusLg；层级 2 = 渐变 hero + radiusXl），SectionCard 内部改为与 Card 主题同款描边、radiusLg（保留 API 不动，调用点零改动）。
- **预期效果**：层次由"写在哪个文件"变为"层级决定"，圆角/描边策略全局一致，后续 UI 改版只需改层级表。

#### A3【P1】色板外内联色：AI 卡紫色渐变

- **现象**：`stats_screen.dart:155-156` AI 入口卡使用 `Color(0xFF6B5B95) / Color(0xFF8B7AB8)` 冷紫色渐变，是全 App 唯一的冷色块，且未进 AppColors，深浅主题共用同一组值。
- **原因**：AI 功能后期追加，直接内联了草稿色。
- **建议**：收入 AppColors（如 `aiCardStart/aiCardEnd`），并按深色模式单独校色；或评估改用品牌陶土渐变 + 图标区分。二选一，先定方向再动手。
- **预期效果**：色板 100% 收敛，暖色语言不被单点破坏。

#### A4【P2】杂散字号游离于字阶之外

- **现象**：AppTheme 定义了 12 档文字样式，但 screens/widgets 仍有 **~70 处裸 `fontSize: N`**（add_record 18 处、cycle_chart 8、period_length_chart 8、home 6、ai 6…），含 10/11px 小字（图例、导航标签、日期徽标）。
- **原因**：图表与细节组件迭代快，"就近写死"比去 AppTheme 加档位顺手。
- **建议**：新增 `AppTheme.micro(10)` / `caption(11)` 两档后全量收敛；顺带复查 <11px 使用的必要性（WCAG 对小字号无硬性下限，但低视力场景应避免 <11px 承载关键信息）。
- **预期效果**：字号变更单点生效；小字可读性有台账。

### B. 响应式适配

#### B1【P2】无断点体系，宽屏形态缺失

- **现象**：全库 `MediaQuery` 仅 4 处（字体钳制、AI 屏键盘 2 处、多选日历 1 处），`LayoutBuilder` 仅 4 处（导航栏滑块、日历高度、2 张图表）——全部是"局部测量"而非"形态切换"。4 tab 布局、hero 卡、3 个 StatCircle、算法选择器全部 Expanded 平铺，在平板/横屏（≥600dp）下会拉成超宽单列。
- **原因**：目标设备一直是 Redmi Note 11 Pro 一档竖屏手机（真机截图驱动），从未定义宽屏形态。
- **建议**：定义 `AppBreakpoints`（600 / 840）；≥600dp 时首页改双栏（左 hero+快速操作 380dp，右日历撑满），统计概览 3 圆环改 Wrap 自适应。图标/导航栏不需动。
- **预期效果**：平板与横屏不再出现"一条卡片横贯 800dp"的失衡布局；无手机端回归风险（断点以下逻辑不变）。

#### B2【P2】日历图例行小屏溢出风险

- **现象**：`home_screen.dart:1344 _buildLegend` 5 个图例项用不换行 Row 平铺，按 5 项 × (8px 圆点 + 间距 + 2~3 字标签) 估算约需 300dp，360dp 以下机型或系统字体 1.3 倍时可能 RenderFlex overflow。
- **建议**：改 `Wrap(spacing: 8, runSpacing: 4)` 一行修复。
- **预期效果**：极小屏 + 大字体组合下不崩版。

#### B3【备注】文本缩放策略——保持现状

根节点钳制 1.0~1.3 + 个别 `TextScaler.noScaling`（历史行徽标等固定容器）均有注释说明理由，策略一致，无需改动；只需在新增固定尺寸容器时延续"要么进钳制、要么注明 noScaling 理由"的约定。

### C. 空间利用率

#### C1【P2】首屏信息密度：完整月网格需滚动 1 屏

- **现象**：首页 hero 卡（含双均值条，约 200dp）+ 快速操作 48dp + 日历卡（6 行 × ~46dp + 标题/图例 ≈ 420dp）≈ 700dp+，而内容视口约 640dp——用户看到日历下半月需滚动。经期中还会再叠经量快选条。
- **建议**（两档任选）：
  - 轻量：滚动越过 hero 后用 SliverAppBar 将 hero 折叠成单行摘要（"第 N 天 · 距下次 X 天"），日历自然上移；
  - 极简：仅给 hero 增加"紧凑开关"（点击双均值条收起），记忆用户偏好。
- **预期效果**：核心路径（看日历/点日期）首屏直达，减少 1 次滚动；视觉改动小、可回退。

#### C2【P3】双历史入口计数口径不一致

- **现象**：记录页历史卡"共 N 条"= `records.length`（含进行中），统计页历史"共 N 条"= `recentPeriods.length`（仅已完成汇总）。同一词"条"两种含义。
- **建议**：短期统一文案口径（如"共 N 条记录 / N 个周期"）；长期差异化定位——记录页负责增删改、统计页负责分析，避免内容重复。
- **预期效果**：信息架构语义清晰，用户不会把"记录数"当"周期数"。

#### C3【P2】布局魔法数互相隐式耦合

- **现象**：底部滚动 clearance `100` 在 4 个屏重复出现；FAB `bottom: 80`；导航栏 `height: 64`；两个 AppBar `toolbarHeight: 68`。这些值互相咬合（clearance ≈ 导航栏 64 + 边距，FAB 80 ≈ 同一基准），但无一处引用共享 token——改导航栏高度需全局搜 100/80/64/68。
- **建议**：`AppDimens` 增加 `navBarHeight=64 / navBarClearance=100 / appBarHeight=68 / fabLift=80` 并全量替换。
- **预期效果**：导航壳尺寸调整单点生效；消除"改一处漏三处"的回归源。

### D. 模块解耦

#### D1【P1】双日历实现并行，重复约 400 行

- **现象**：首页单选日历（PageView + 42 格网格 + 长按详情）与多选日历页（滚动月份列表 + 区间选择）是两套完整实现（合计 ~2,200 行），以下能力各写了一遍：月份天数/前置空位计算、非本月置灰格、星期标题行、日格圆角描边样式、日类型配色分发。此前 P1-7 收敛 flowOptions 的注释正是"改了弹窗忘了状态卡"这类双处同步痛点的实证。
- **建议**：抽 `widgets/calendar/`——`MonthGrid`（纯网格生成器，输入年月+回调）+ `DayCell`（渲染协议：fill/ring/text 样式由调用方给）+ 选择策略接口（单选 / 区间多选两个实现）。首页与多选页各自注入策略与数据源。
- **预期效果**：约 -400 行重复；"日历视觉规则"从两处改为一处；未来新增日历场景（如某天详情页）边际成本趋近于零。**注意**：属结构性重构，需在 analyze/156 测试 + 真机截图回归保护下分两个独立 commit 做（先抽核、后换首页）。

#### D2【P1】AI 屏：业务逻辑沉在 View 层

- **现象**：`ai_assistant_screen.dart` 1542 行，State 内直接实现流式回放节奏控制（`_flushStreamingBuffer` / `_scheduleStreamFlush` / `_cancelFlushTimer`）、会话切换/清空/历史弹层、报告生成编排与步骤动画。Provider 只负责存储，回放节奏（12 字/60ms、>120 字切打字机）这一**核心算法**却活在 UI 定时器里——无法单测，且 1.36.1 调参时只能改 UI 文件。
- **原因**：流式平滑回放是后加能力，当时从改动最小路径落在了屏幕文件。
- **建议**：① 回放节奏逻辑（chunk 切分 + flush 调度）下沉 `AiAssistantProvider`，屏幕只订阅展示；② UI 拆 4 个私有组件文件：chat_bubble / report_view / model_selector / session_sheet，主文件回落到 <600 行。
- **预期效果**：回放公式可单测（配合现有 test 体系），1.36.1 那类调参不再触碰 1500 行文件；聊天 UI 迭代与逻辑解耦。

#### D3【P2】PeriodProvider"上帝化"（1139 行、6 类职责）

- **现象**：一个类同时负责：数据加载/缓存失效、日类型计算、**3 路通知同步**（period/daily/ovulation）、**小组件推送**、午夜刷新、导入导出。通知与小组件属于"副作用服务"，与数据状态强耦合导致每次通知逻辑调整都进这个最大文件。
- **建议**：拆 `NotificationSyncCoordinator` 与 `WidgetSyncService`（Provider 持有并委托调用，公开 getter 不变，调用点零改动）。现有 13 个测试文件是安全网。
- **预期效果**：主文件回落 ~700 行；通知/小组件逻辑可独立 mock 测试；与 1.34.0 通知架构（按 ID 精确取消、_syncChain 串行）的约定集中到一处。

#### D4【P2】全局 GlobalKey 跨页导航

- **现象**：`home_screen.dart:242` 通过 `MainScreen.globalKey.currentState?.jumpToSettings()` 跳 tab。全局可变单例 + 依赖壳层具体方法名，测试与重构时脆弱。
- **建议**：轻量方案——tab 索引放一个 `ValueNotifier<int>` 或由 app.dart 注入回调；不必为此引入路由库（当前仅 4 tab + 2 push，不满足上 GoRouter 的复杂度门槛）。
- **预期效果**：去掉一处全局可变状态，导航行为可测。

### E. 可维护性

#### E1【P1】StatCircle 重复实现两份

- **现象**：`common_widgets.dart:58 StatCircle` 与 `stats_screen.dart:350 _buildStatCircle` 几乎逐行相同（76px 圆 + 数值/单位 + 下方标签），后者多一处可配色但参数未用全。
- **建议**：保留 common_widgets 版本（补上 stats 需要的参数），stats_screen 改引用。
- **预期效果**：-40 行重复；76px/字号调整单点生效。

#### E2【P2】AppStrings 集中管理被绕过

- **现象**：settings/record/stats 等屏仍有内联中文：`'恢复自动备份' '暂无自动备份' '选择备份' '最新' '重试' '已保存 N 条记录' '共 N 条' '持续' '至' '当有足够的历史记录时…' '导出失败: $e'` 等约 20 处。
- **建议**：一次性收尾进 AppStrings（含带占位符的按 `replaceAll('{}')` 约定）。
- **预期效果**：文案改动 100% 单点；未来如做多语言有基础。

#### E3【P2】UI 层零测试

- **现象**：13 个测试文件全部覆盖 provider/service/纯函数；所有 UI 回归依赖真机截图人工走查。
- **建议**：不必上 golden 全家桶，先加三类基础 widget test：①日历格子五态渲染断言（period/predicted/selected/today/otherMonth）；②底部导航切换 + IndexedStack 保活断言；③图例/今日条等高价值小组件。与 A1/B2 的修复配套编写，验证即留存。
- **预期效果**：UI 回归从"每次真机全手点"变为"测试先筛一遍 + 截图抽查"。

#### E4【P3】AGENTS.md 文档漂移

- **现象**：AGENTS.md 描述 `AppDimens`、`AppThemeColors` 为 constants 下独立文件——实际都在 `app_colors.dart`；widgets 清单缺 report_cards / period_edit_dialog / 4 张图表 / trend_chart_shared；`MainScreen` 壳层在 app.dart 未提及。
- **建议**：文档同步一次（10 分钟）。因新会话依赖 AGENTS.md 建立心智模型，漂移会传导成错误假设。
- **预期效果**：AI 协作/新人上手不再踩"按文档找文件"的坑。

#### E5【P3】app.dart 混入壳层 UI

- **现象**：app.dart 后 240 行是 `MainScreen + _GlassNavBar + _NavItem`（含导航项定义、滑块动画、Semantics）。
- **建议**：整体移至 `screens/main_screen.dart`，app.dart 回归"根装配（Provider + MaterialApp + 生命周期 + 字体钳制）"单一职责。
- **预期效果**：根文件 <160 行；导航壳独立演进不碰根装配。

---

## 三、优先级路线图

| 级别 | 事项 | 维度 | 工作量 | 理由 |
|---|---|---|---|---|
| **P0** | A1 深色模式 ink* 对比度修复（36 处机械替换 + 真机走查） | 视觉/无障碍 | ~0.5 天 | 正确性缺陷：深色模式不可读，替换规则明确、风险低 |
| **P0** | A3 AI 卡紫色收入 AppColors | 视觉 | ~0.5h | 色板外唯一冷色，一处改动 |
| **P1** | E1 StatCircle 合并 | 解耦/可维护 | ~0.5h | 顺手清理，为 A2 表面统一热身 |
| **P1** | A2 表面层级表统一（SectionCard 对齐 Card 主题） | 视觉 | ~0.5 天 | API 不变只改内部，调用点零改动 |
| **P1** | D2 AI 屏拆分 + 流式回放下沉 Provider | 解耦 | 1~2 天 | 1542 行最大文件，回放算法获得可测性 |
| **P1** | D1 双日历抽核 | 解耦 | 2~3 天 | -400 行重复；需拆 2 个 commit + 截图回归 |
| **P2** | B2 图例 Wrap 防溢出 | 响应式 | ~0.5h | 一行修复 |
| **P2** | C3 布局魔法数 token 化 | 可维护 | ~1h | 纯机械替换 |
| **P2** | E2 AppStrings 收尾 | 可维护 | ~1h | 纯机械替换 |
| **P2** | C1 首屏 hero 折叠 | 空间 | 1 天 | 视觉改动，建议先出紧凑态设计再动手 |
| **P2** | B1 宽屏断点双栏 | 响应式 | 1~2 天 | 无真机平板验证条件前可降级为"预留断点常量" |
| **P2** | D3 PeriodProvider 拆副作用服务 | 解耦 | 1 天 | 测试安全网已备 |
| **P2** | E3 基础 widget test | 可维护 | 1 天 | 与 P0/P1 修复配套写 |
| **P3** | D4 全局 Key 换注入 | 解耦 | ~2h | |
| **P3** | E4 AGENTS.md 同步 / E5 app.dart 壳外移 / C2 历史口径 | 可维护 | 各 <1h | |

**验收标准（每批改动通用）**：`flutter analyze` 0 issue → 156+ 测试全绿（新增测试并入）→ 真机截图走查（P0 必须含深色模式）→ APK 构建通过 → 版本号按规范递增 → 多组独立改动拆分本地 commit。

---

## 四、量化附录

| 指标 | 数值 | 说明 |
|---|---|---|
| lib 总行数 | 17,890 / 37 文件 | |
| 最大 UI 文件 | ai_assistant_screen 1542、home_screen 1442 | 均 >Flutter 常见 600 行拆分阈值 |
| AppColors.ink* 用于主题表面 | 36 处（record 16 / home 12 / ai 8） | P0 修复对象；固定浅色面上的合法用法需逐处甄别 |
| 裸 fontSize（token 外） | ~70 处 | add_record 18、图表 16、home 6、ai 6… |
| screens 裸数字 SizedBox/EdgeInsets | ~28 处 | 另有底部 clearance 100 ×4、FAB 80、导航 64、AppBar 68 ×2 |
| MediaQuery / LayoutBuilder | 4 处 / 4 处 | 无一处用于形态断点 |
| StatCircle 实现 | 2 份（common_widgets + stats_screen） | 逐行近重复 |
| UI/widget 测试 | 0（13 个测试全在 provider/service/纯函数层） | |

### 预期改进效果总览

1. **可访问性**：深色模式正文对比度 ~1.2:1 → ≥4.5:1（P0，唯一"缺陷级"问题）；
2. **一致性**：色板/字阶/圆角 100% 归口 token，表面层级显式化；
3. **代码量**：日历抽核 -400 行、StatCircle -40 行、AI 屏 -900 行（拆分后主文件 <600）；
4. **可测性**：流式回放公式、日历五态、导航保活进入自动化测试；
5. **演进成本**：日历/文案/布局尺寸/通知逻辑改动均单点生效，UI 改版期的"改一处漏三处"风险显著下降。
